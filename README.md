# Trip.Api

## Project introduction

This is an api project imitating trip.com

## Build and run this project

If you want to build this project, just need use following command:

```bash
dotnet build
```

And if you want to run this project, just use following command:

```bash
dotnet run
```